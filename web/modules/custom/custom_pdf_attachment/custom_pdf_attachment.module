<?php

use Drupal\commerce_order\Entity\OrderInterface;
use Drupal\commerce_product\Entity\ProductVariationInterface;
use Drupal\file\Entity\File;

/**
 * Preprocess the order receipt email template.
 */
function custom_pdf_attachment_preprocess_commerce_order_receipt(array &$variables) {
  $order = $variables['order_entity'];
  if (!$order instanceof OrderInterface) {
    return;
  }

  $download_links = [];

  foreach ($order->getItems() as $order_item) {
    /** @var ProductVariationInterface $variation */
    $variation = $order_item->getPurchasedEntity();
    if ($variation->hasField('commerce_file')) {
      foreach ($variation->get('commerce_file') as $field_item) {
        $file = $field_item->entity;
        if ($file instanceof File) {
          $download_links[] = [
            'title' => $file->getFilename(),
            'url' => file_create_url($file->getFileUri()),
          ];
        }
      }
    }
  }

  $variables['download_links'] = $download_links;
}